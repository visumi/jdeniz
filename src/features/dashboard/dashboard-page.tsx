import { Activity, ArrowRight, Plus, Sparkles, Users } from "lucide-react";
import { Link } from "react-router-dom";
import { Button } from "../../components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "../../components/ui/card";
import { Skeleton } from "../../components/ui/skeleton";
import { useStudents } from "../../hooks/use-students";
import { formatDate } from "../../lib/utils";

export function DashboardPage() {
  const students = useStudents();
  const recentStudents = students.data?.slice(0, 5) || [];
  return <div className="space-y-8"><section className="grid gap-5 lg:grid-cols-[minmax(0,1.3fr)_minmax(18rem,0.7fr)]"><div className="relative isolate overflow-hidden rounded-2xl bg-slate-950 px-6 py-7 text-white sm:px-8 sm:py-9"><div className="relative z-10 max-w-2xl space-y-4"><div className="flex items-center gap-2 text-sm font-semibold text-sky-300"><Sparkles className="size-4" />Painel de hoje</div><h1 className="text-3xl font-bold leading-tight tracking-[-0.03em] sm:text-4xl">Pronto para cuidar da sua próxima sessão?</h1><p className="max-w-lg text-base leading-7 text-slate-300">Sua base está aqui para deixar cada atendimento mais leve, organizado e pessoal.</p><Button asChild className="bg-sky-500 text-white hover:bg-sky-400"><Link to="/students?new=1"><Plus className="size-4" />Cadastrar aluno</Link></Button></div><div aria-hidden="true" className="absolute -right-20 -top-20 size-64 rounded-full border border-sky-400/20" /><div aria-hidden="true" className="absolute -bottom-32 right-16 size-72 rounded-full border border-white/10" /></div><div className="flex flex-col justify-between gap-8 rounded-2xl border border-sky-200 bg-sky-50 p-6"><div className="flex items-center justify-between"><div><p className="text-sm font-semibold text-sky-800">Sua base</p><p className="mt-1 text-sm text-sky-700/80">Alunos acompanhados</p></div><div className="grid size-10 place-items-center rounded-xl bg-white text-sky-600"><Users className="size-5" /></div></div><div className="flex items-end gap-2"><span className="text-5xl font-bold tracking-[-0.04em] text-sky-950">{students.isPending ? "—" : students.data?.length || 0}</span><span className="pb-1 text-sm font-medium text-sky-800/70">neste espaço</span></div><Button asChild variant="ghost" className="w-full justify-between bg-white/70 text-sky-800 hover:bg-white hover:text-sky-950"><Link to="/students">Explorar alunos <ArrowRight className="size-4" /></Link></Button></div></section><section className="space-y-4"><div className="flex items-end justify-between gap-4"><div><div className="flex items-center gap-2 text-sm font-semibold text-sky-700"><Activity className="size-4" />Movimento recente</div><h2 className="mt-1 text-xl font-bold tracking-tight">Últimos cadastros</h2><p className="mt-1 text-sm text-muted-foreground">Acesse rapidamente quem recebeu atenção por último.</p></div><Button asChild variant="ghost" className="hidden shrink-0 sm:inline-flex"><Link to="/students">Ver todos <ArrowRight className="size-4" /></Link></Button></div><Card className="overflow-hidden"><CardHeader className="border-b border-border/70 bg-white/70"><CardTitle>Base de alunos</CardTitle><CardDescription>Cadastros que você pode consultar e atualizar.</CardDescription></CardHeader><CardContent className="p-0">{students.isPending ? <div className="space-y-3 p-5"><Skeleton className="h-12 w-full" /><Skeleton className="h-12 w-full" /></div> : students.isError ? <p className="m-5 rounded-lg bg-destructive/10 p-3 text-sm leading-5 text-destructive">Não foi possível carregar seus alunos agora.</p> : recentStudents.length === 0 ? <div className="px-4 py-10 text-center"><p className="font-semibold">Sua base ainda está vazia.</p><p className="mt-1 text-sm text-muted-foreground">Cadastre seu primeiro aluno para começar.</p><Button asChild variant="secondary" className="mt-4"><Link to="/students?new=1">Cadastrar primeiro aluno</Link></Button></div> : <div className="divide-y divide-border/70">{recentStudents.map((student) => <Link key={student.id} to={`/students/${student.id}`} className="flex items-center justify-between gap-4 px-5 py-4 transition-colors hover:bg-sky-50/70"><div className="flex min-w-0 items-center gap-3"><div className="grid size-9 shrink-0 place-items-center rounded-lg bg-sky-100 text-sm font-bold text-sky-700">{student.name.charAt(0).toUpperCase()}</div><div className="min-w-0"><p className="truncate font-semibold">{student.name}</p><p className="text-xs text-muted-foreground">Atualizado em {formatDate(student.updatedAt)}</p></div></div><ArrowRight className="size-4 shrink-0 text-slate-400" /></Link>)}{students.data.length > recentStudents.length && <div className="p-3 sm:hidden"><Button asChild variant="link" className="px-2"><Link to="/students">Ver todos os alunos <ArrowRight className="size-4" /></Link></Button></div>}</div>}</CardContent></Card></section></div>;
}
